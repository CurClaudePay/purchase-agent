{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agent-identity.schema.json",
  "title": "AgentIdentity",
  "description": "Who the agent is, who it acts for and how its requests are verified.",
  "type": "object",
  "required": ["agentId", "consumerId", "provider", "publicKey", "status"],
  "properties": {
    "agentId": { "type": "string" },
    "consumerId": { "type": "string", "description": "The consumer the agent acts for." },
    "provider": { "type": "string", "description": "Who built and operates the agent." },
    "publicKey": { "type": "string", "description": "Key used to verify the agent's signed requests." },
    "registeredAt": { "type": "string", "format": "date-time" },
    "status": { "enum": ["active", "suspended", "revoked"] }
  }
}
