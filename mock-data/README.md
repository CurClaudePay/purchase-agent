# Mock data

Shared fictional data used by every team. Everything here is made up: no real
people, addresses, IBANs or devices. Change it only in agreement with the teams
that use it.

- `coffee-machine.json`: the connected machine and its capsule level
- `products.json`: the merchant's capsule catalogue
- `accounts.json`: the consumer, the merchant and their sandbox wallets
