# Merchant

Catalogue, offer, order and delivery for the fictional coffee shop
(`merch-001` in `mock-data/accounts.json`). Serves products from
`mock-data/products.json`, returns offers to the consumer agent and confirms
orders only after the wallet reports settlement.
